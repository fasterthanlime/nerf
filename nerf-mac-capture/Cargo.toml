[package]
name = "nerf-mac-capture"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Trait + event types shared between the macOS daemon recorder and its consumers (live aggregator)."
